#include "bitpacker.h"

bitset<8> to_bitset(unsigned char byte)
{
    return bitset<8>(byte);
}

bits code2bits(tcode code){

}

bits codes2bits(vector<tcode> codes){

}

void hep(){
  cout << "hep4anskij" << endl;
}
